const uploadHandlers = require('../handlerrs/handleUpload');

describe('handUpload', function() {
  describe('start', function() {
    test('it creates a file with the name field', function() {});

    test('when no name field is included it does not create a file', function() {});

    test('when no name field is included it emits uploadfailed', function() {});
  });

  describe('data', function() {
    test('it streams data into the named file', function() {});
  });

  describe('end', function() {
    test('it emits uploadsuccess', function() {});
  });
});
